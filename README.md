# Redis_Assignment_Node2  
## Instructions to run  
Before running the program, install node modules.  
Then run index.js in the terminal, followed by publisher.js in a separate terminal to see the Pub Sub Communication
